# Leave empty when the frontend is served by Flask or through Vite's proxy.
VITE_API_BASE_URL=

# Used only by Vite's development proxy; do not include credentials here.
VITE_BACKEND_URL=http://localhost:5000
